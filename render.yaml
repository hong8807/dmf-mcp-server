services:
  - type: web
    name: dmf-intelligence
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 8000
      - key: SERVER_MODE
        value: kakao
